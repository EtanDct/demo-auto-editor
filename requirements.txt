# Piper (tts.engine: piper, alternative à Kokoro) n'est pas dans ce fichier :
# piper-tts (PyPI) dépend de piper-phonemize, qui n'a pas de wheel Windows.
# scripts/download_models.py télécharge à la place le binaire officiel
# (rhasspy/piper releases), invoqué en CLI par scripts/build_narration.py.
faster-whisper==1.0.3
huggingface_hub==0.25.2
llama-cpp-python==0.3.2
pydantic==2.9.2
PyYAML==6.0.2
typer==0.27.2
numpy==2.4.6
psutil==6.0.0

# Voix off Kokoro (tts.engine: kokoro). Tourne sur onnxruntime, déjà tiré par
# l'OCR, et embarque espeak-ng dans ses dépendances : rien à installer côté
# système, contrairement à piper-tts. Exige numpy >= 2.
kokoro-onnx==0.6.1

# OCR local pour l'étape `screen` (index du texte affiché à l'écran).
# rapidocr-onnxruntime tire onnxruntime et opencv-python ; aucun binaire
# système à installer, et aucun appel réseau une fois les modèles en cache.
rapidocr-onnxruntime==1.4.4

# Développement : tests de la logique métier (recalage, sous-titres, filtres).
#   python -m pytest tests
pytest==8.3.3
